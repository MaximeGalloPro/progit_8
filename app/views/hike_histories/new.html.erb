<div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 py-6">
    <div class="mb-6">
        <h1 class="text-3xl font-bold text-white">
            <% if params[:past] == "true" %>
                Ajouter une randonnée passée
            <% else %>
                Ajouter une randonnée à venir
            <% end %>
        </h1>
    </div>
    <%= render partial: "form" %>
    <%= render partial: "guide_modal" %>
</div>

<script type="module">
    import "modal"
    import "guide_modal"
</script>
